# Vite loads env files from this directory. Copy to .env
VITE_API_URL=http://localhost:5000

# Optional: dev server port (default 5173)
# VITE_DEV_PORT=5173

# Optional: subpath when hosting the SPA under a folder (cPanel subfolder deploy)
# BASE_PATH=/
